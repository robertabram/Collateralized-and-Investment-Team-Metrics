<-- Period Dynamic Slicer -->
ChooseDisbPeriod = 
VAR Period = SELECTEDVALUE(SelectorSecured[Period], "All Period")

RETURN
SWITCH(
    Period,
    "Last 3 Months",
        IF(
            MAX(
            LoanDisbursement[FACT_DATE]) >= DATE(
        YEAR(TODAY()),MONTH(TODAY())-3,1),1,0),1)
